---
title: 'ReactJS CheatSheet: Técnicas Essenciais'
publishedAt: 2025-03-30
language: 🇧🇷
---

## 💡 Introdução

Guia rápido para lembrar os conceitos principais do React. Aqui, vamos explorar técnicas essenciais para trabalhar com componentes funcionais, hooks, eventos e renderização condicional. Tudo de forma simples e direta.

## ⚙️ Pré-requisitos

Para aproveitar melhor este guia, é bom ter conhecimentos básicos de:

- JavaScript (ES6+);
- Noções de React;
- Como criar componentes funcionais;

Se esses conceitos ainda são novos para você, recomendo dar uma olhada na [documentação oficial do React](https://react.dev/) antes de seguir.

---

## 1. Componentes e Propriedades

👉 **Componentes funcionais**: são a base do React. Eles recebem **propriedades** (props) e retornam a interface da aplicação.

```jsx
function MyComponent(props) {
  return <div>Olá, {props.name}!</div>;
}
```

👉 **Desestruturando propriedades**: uma forma mais limpa e prática de acessar os valores das props:

```jsx
function MyComponent({ name }) {
  return <div>Olá, {name}!</div>;
}
```

## 2. Hooks Essenciais

👉 **`useState`**: utilizado para gerenciar estados no React. 

```jsx
import { useState } from "react";

function Contador() {
  const [contador, setContador] = useState(0);

  return (
    <div>
      <p>Valor: {contador}</p>
      <button onClick={() => setContador(contador + 1)}>Incrementar</button>
    </div>
  );
}
```

👉 **`useEffect`**: utilizado para manipular efeitos colaterais em uma aplicação React, como chamadas de API por exemplo.

```jsx
import { useState, useEffect } from "react";

function Clock() {
  const [hour, setHour] = useState(new Date());

  useEffect(() => {
    const interval = setInterval(() => {
      setHour(new Date());
    }, 1000);

    return () => clearInterval(interval); // Cleanup do efeito
  }, []);

  return <p>Hora atual: {hour.toLocaleTimeString()}</p>;
}
```

👉 **`useContext`**: utilizado para compartilhar estados globalmente em uma aplicação

```jsx
import { createContext, useContext } from "react";

const UserContext = createContext();

function Saudacao() {
  const user = useContext(UserContext);
  return <p>Bem-vindo, {user.name}!</p>;
}

function App() {
  return (
    <UserContext.Provider value={{ name: "Danilo" }}>
      <Saudacao />
    </UserContext.Provider>
  );
}
```

👉 **`useReducer`**: utilizado para gerenciar estados complexos em uma aplicação.

```jsx
import { useReducer } from "react";

function reducer(state, action) {
  switch (action.type) {
    case "increment":
      return { count: state.count + 1 };
    case "decrement":
      return { count: state.count - 1 };
    default:
      return state;
  }
}

function Contador() {
  const [state, dispatch] = useReducer(reducer, { count: 0 });

  return (
    <div>
      <p>Contador: {state.count}</p>
      <button onClick={() => dispatch({ type: "increment" })}>+</button>
      <button onClick={() => dispatch({ type: "decrement" })}>-</button>
    </div>
  );
}
```

👉 **`useRef`**: utilizado para criar uma referência a elementos do DOM.

```jsx
import { useRef } from "react";

function FocusInput() {
  const inputRef = useRef(null);

  function handleClick() {
    inputRef.current.focus();
  }

  return (
    <div>
      <input ref={inputRef} type="text" placeholder="Digite algo..." />
      <button onClick={handleClick}>Focar</button>
    </div>
  );
}
```

## 3. Lidando com Eventos

👉 **Eventos do usuário**: como funciona as interações isoladas com elementos de uma aplicação.

```jsx
function Button() {
  function handleClick() {
    console.log("Botão clicado!");
  }

  return <button onClick={handleClick}>Clique aqui</button>;
}
```

👉 **Eventos de formulário**: como funciona as interações com elementos de uma formulário, como inputs, botões, etc.

```jsx
import { useState } from "react";

function Form() {
  const [inputValue, setInputValue] = useState("");

  function handleChange(event) {
    setInputValue(event.target.value);
  }

  return (
    <input
      type="text"
      value={inputValue}
      onChange={handleChange}
      placeholder="Digite algo..."
    />
  );
}
```
## 4. Renderização Condicional

👉 **Operador ternário**: uma forma simples de criar uma condição boleana para manipular a exibição de elementos de interface na aplicação.

```jsx
function Dashboard({ isLogged }) {
  return isLogged ? <UserDashboard /> : <GuestDashboard />;
}
```

👉 **Curto-circuito (`&&`)**: uma forma mais direta que exibir um elemento de interface baseada em expressão como condicional.

```jsx
function Mensagem({ isLogged }) {
  return isLogged && <p>Bem-vindo de volta!</p>;
}
```

---

Por hoje é isso, espero que este guia tenha sido útil para você. 

Bom código! 🚀