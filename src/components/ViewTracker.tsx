"use client";

interface ViewTrackerProps {
  slug: string;
}

export function ViewTracker({ slug }: ViewTrackerProps) {
  // View tracking can be re-implemented with a local solution
  // (e.g. Vercel Analytics, Plausible, or a database)
  return null;
}
